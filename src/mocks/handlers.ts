// Example MSW handlers for Smile_fashion_brand
// You should customize these handlers for your API endpoints

export const handlers = [
  // Example handler
  // rest.get('/api/example', (req, res, ctx) => {
  //   return res(ctx.status(200), ctx.json({ message: 'Hello from MSW!' }));
  // }),
];
